interface:
  display_name: "PassiveOSINTControlPanel"
  short_description: "Repo-specific patterns and workflows for PassiveOSINTControlPanel"
  default_prompt: "Use the PassiveOSINTControlPanel repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true